#include<bits/stdc++.h>

using namespace std;

int main () {
	ios_base::sync_with_stdio(false);
	cin.tie(NULL);
	int tt;
	cin >> tt;
	while(tt--){
    int n;
    cin >> n;
    vector<int> v(n);
    for(auto &x : v) cin >> n;
    string s;
    cin >> s;
    map<int, int> mp;
    for(auto x : v) mp[x]++;
	}
}
