#include<bits/stdc++.h>

using namespace std;

int main () {
	ios_base::sync_with_stdio(false);
	cin.tie(NULL);
	int tt;
	cin >> tt;
	while(tt--){
    int n, x;
    cin >> n >> x;
    vector<int> v(n);
    for(int &i : v) cin >> i;
    vector<vector<int>> sa;
    // 1 2 2 3 2 2 3 3
    for(int i = 0; i < v.size(); i++) {
      for(int j = 0; j < v.size(); j++){

      }
    }
	}
}
